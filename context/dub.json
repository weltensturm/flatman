{
	"name" : "flatman-context",
	"description" : "per-workspace current directory for flatman",
	"targetType": "executable",
	"sourcePaths": ["src"],
	"dependencies": {
		"ws": "18.10.1"
	},

	"configurations": [
		{ "name": "default" },
		{ "name": "unittest" }
	]
	
}
